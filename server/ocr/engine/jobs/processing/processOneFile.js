export async function processOneTableOneFile(
  r,
  params,
  engineOverrides,
  context
) {
  const {
    PHOTOS_DIR,
    SCREEN_SCALE,
    getCalibration,
    RULES_PATH
  } = context;

  // ← existing logic unchanged
}